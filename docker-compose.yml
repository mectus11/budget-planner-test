services:
  budget-planner:
    build: .
    image: budget-planner
    container_name: budget-planner
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=5000
    # Uncomment if using a real database in the future
    # environment:
    #   - DATABASE_URL=postgresql://user:password@db:5432/budget_planner
