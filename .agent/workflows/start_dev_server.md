---
description: Start the development server for the Budget Planner application.
---

This workflow installs dependencies (if needed) and starts the development server.

1. Install dependencies if node_modules is missing.
   // turbo
   npm install

2. Start the development server.
   // turbo
   cmd /c npm run dev
